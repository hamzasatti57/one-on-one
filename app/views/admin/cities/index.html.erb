<div class="card">
  <div class="card-header custom-card-header">
    <i class="fa fa-align-justify"></i> Listing all Cities</div>
  <div class="card-body">
    <div class="row">
      <div class="col-lg-12">
        <table id="city-table" class="table table-striped table-bordered display">
          <thead>
          <tr>
            <th>Sr#</th>
            <th>Name</th>
            <th>Actions</th>
          </tr>
          </thead>
          <tbody>
          <% @cities.each_with_index do |city, index| %>
            <tr>
              <td><%= index+1 %></td>
              <td><%= city.title %></td>
              <td><%= link_to "Edit",  edit_admin_city_path(city), class: "btn btn-xs btn-primary custom-table-edit-btn" %>
                <%= link_to "Show",  admin_city_path(city), class: "btn btn-xs btn-success custom-table-show-btn" %>
                <%= link_to "Delete", admin_city_path(city), method: :delete , data: {confirm: "Are you sure?"} , class: "btn btn-xs btn-danger custom-table-delt-btn" %></td>
            </tr>
          <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>


<script>
    $(document).ready( function () {
        $('#city-table').dataTable();
    } );
</script>
