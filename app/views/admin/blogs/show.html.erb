<div class="row">
  <div class="col-lg-9 mt-3 offset-2">
    <div class="card">
      <div class="card-header">
        <strong>Blog</strong>
      </div>
      <div class="card-body">
        <h2 align="center">Blog: <%= @blog.title %></h2>
        <div class="well col-xs-8 col-xs-offset-2">
          <div class="Center">
            <% if @blog.image.attached? %>
              <%= image_tag(url_for(@blog.image.variant(resize: '200x200'))) %><br>
            <% end %>
          </div>
          <br>
          <h4>Description: </h4><br>
          <p><%= @blog.description%></p>
          <div class="article-actions">
            <%= link_to "Edit",  edit_admin_blog_path(@blog), class: "btn btn-xs btn-primary" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
